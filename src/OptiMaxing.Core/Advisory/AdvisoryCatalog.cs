namespace OptiMaxing.Core.Advisory;

/// <summary>
/// "Advisory Only" items (spec п.23/M8): things that genuinely help FPS/latency but
/// cannot be safely automated — BIOS-only settings, physical maintenance, per-game
/// settings. OptiMaxing explains what and why instead of pretending to fix them.
/// </summary>
public sealed record AdvisoryEntry(string Title, string Advice);

public static class AdvisoryCatalog
{
    public static readonly IReadOnlyList<AdvisoryEntry> Entries =
    [
        new("XMP/EXPO в BIOS",
            "Без включения профиля памяти в BIOS оперативная память обычно работает на заводской JEDEC-частоте (например 2133-2666 МГц вместо заявленных 3200-6000). Разница в играх, чувствительных к пропускной способности памяти (симуляторы, стратегии), может быть заметной. OptiMaxing не может включить это программно — зайди в BIOS/UEFI и включи XMP (Intel) или EXPO (AMD)."),

        new("Resizable BAR / Above 4G Decoding",
            "Даёт CPU прямой доступ ко всей видеопамяти вместо кусков по 256 МБ — в поддерживающих играх прирост FPS. Требует поддержки материнской платой, CPU и видеокартой, и включается только в BIOS."),

        new("Термопаста и охлаждение",
            "Если CPU/GPU троттлят под нагрузкой (частота проседает при высокой температуре) — ни один программный твик это не исправит. Раз в 2-4 года стоит проверить/заменить термопасту и почистить радиаторы от пыли."),

        new("Переустановка драйвера видеокарты через DDU",
            "При смене AMD↔NVIDIA или после нестабильности рекомендуется полная чистая переустановка драйвера через Display Driver Uninstaller в безопасном режиме — обычная переустановка поверх старой иногда оставляет конфликтующие файлы."),

        new("Игровые настройки: шейдеры/тени/отражения важнее разрешения",
            "Если нужно больше FPS — в большинстве игр тени, отражения (RT) и объёмный туман/дым стоят дороже всего по производительности при малом визуальном отличии на среднем уровне. Разрешение и текстуры снижай в последнюю очередь — они сильнее всего влияют на чёткость картинки."),

        new("Мониторинг вместо угадывания",
            "Прежде чем гнаться за очередным твиком — посмотри, где реально узкое место (CPU/GPU/VRAM/температуры) через HWiNFO или CapFrameX. Часто 'тормозит' конкретный компонент, и твик из общего списка просто не по адресу."),
    ];
}
